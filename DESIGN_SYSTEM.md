# Design System

> 이 문서는 AI(Claude 등)에게 UI를 만들게 할 때 그대로 전달하는 자기완결형 디자인 시스템이다.
> 새 프로젝트의 CLAUDE.md에서 이 파일을 참조하거나, 통째로 붙여넣어 사용한다.
> 판단 기준이 되는 상위 원칙은 [README.md](README.md)의 3대 원칙 문서를 따른다.

## 0. 디자인 방향 (모든 결정의 기준)

| 항목 | 결정 | 이유 |
|------|------|------|
| 인상 | 깔끔하고 신뢰감 있는 (토스 스타일) | 제품의 기본 성격 |
| 대상 | 일반 대중, 전 연령 | 글자 크게, 용어 쉽게, 터치 영역 넉넉하게 |
| 모드 | 라이트 + 다크 모두 지원 | 모든 색은 토큰으로만 사용 |
| 밀도 | 여유롭게 | 한 화면에 한 가지 목적, 핵심만 |
| 말투 | 해요체, 이모지 없음, 사용자 호칭 없음 | 차분하고 신뢰감 있는 톤 |
| 모션 | 자연스럽게 최소한 (150~250ms) | 있는 듯 없는 듯 |

**한 줄 요약: 토스처럼 담백하고 믿음직하게. 화려함보다 명료함.**

## 1. 색상 (Color Tokens)

색은 반드시 토큰으로만 사용한다. hex 값을 컴포넌트에 직접 쓰지 않는다.

### 1.1 브랜드 색 — Blue (기준: #317FEE)

| 토큰 | 값 | 용도 |
|------|-----|------|
| blue-50 | #F0F6FE | 연한 배경 (선택된 항목 배경 등) |
| blue-100 | #DCEAFD | 배지 배경, hover 배경 |
| blue-200 | #B9D5FB | 보조 장식 |
| blue-300 | #8FBBF8 | 비활성 상태의 브랜드 요소 |
| blue-400 | #5D9CF3 | 다크 모드의 primary |
| blue-500 | #317FEE | 브랜드 기준색. 넓은 배경·장식 전용 (흰 글자 3.89:1 → 텍스트 금지) |
| **blue-600** | **#2368CC** | **primary. 주요 버튼, 링크, 강조 (흰 글자 5.35:1)** |
| blue-700 | #1B52A3 | primary pressed/hover (7.55:1) |
| blue-800 | #153F82 | — |
| blue-900 | #102E5E | — |

### 1.2 중립 색 — Gray

| 토큰 | 값 | 용도 |
|------|-----|------|
| gray-50 | #F9FAFB | 페이지 배경(라이트) |
| gray-100 | #F2F4F6 | 카드 구분 배경, 비활성 버튼 배경 |
| gray-200 | #E5E8EB | 경계선(border), 구분선 |
| gray-300 | #D1D6DB | 진한 경계선, 비활성 아이콘 |
| gray-400 | #B0B8C1 | placeholder |
| gray-500 | #8B95A1 | 보조 중 보조 텍스트 |
| gray-600 | #636D7A | 보조 텍스트 (캡션, 설명). bg-fill 위에서도 4.76:1 |
| gray-700 | #4E5968 | 본문 텍스트(약한 강조) |
| gray-800 | #333D4B | 본문 텍스트 |
| gray-900 | #191F28 | 제목 텍스트 |

### 1.3 의미 색 — Semantic

| 토큰 | 값 | 용도 |
|------|-----|------|
| red-500 | #F04452 | 오류 그래픽·일러스트 (흰 글자 3.71:1 → 텍스트 금지) |
| **red-600** | **#C92A37** | **danger. 삭제 버튼, 오류 텍스트 (흰 글자 5.43:1)** |
| red-50 | #FEF0F1 | 오류 배경 |
| green-500 | #1FA45B | 성공 그래픽·일러스트 |
| **green-700** | **#12793F** | **성공 텍스트·아이콘 (흰 배경 5.48:1)** |
| green-50 | #EDF9F2 | 성공 배경 |
| orange-500 | #ED8B00 | 주의 그래픽 (흰 배경 2.53:1 → 텍스트·단독 아이콘 금지) |
| **orange-700** | **#A85B00** | **주의 텍스트·아이콘 (흰 배경 5.05:1)** |
| orange-50 | #FEF6EA | 주의 배경 |

빨강은 오류·삭제에만 쓴다. 강조하고 싶다고 빨강을 쓰지 않는다(강조는 primary).

**-500 원색은 넓은 면적의 배경·그래픽 전용이다.** 글자와 작은 아이콘에는 진한 톤
(blue-600, red-600, green-700, orange-700) 또는 §1.4의 시맨틱 토큰만 쓴다.

### 1.4 라이트/다크 시맨틱 토큰

컴포넌트는 아래 시맨틱 토큰만 사용한다. 모드 전환은 이 층에서만 일어난다.

| 시맨틱 토큰 | 라이트 | 다크 | 용도 |
|------------|--------|------|------|
| bg-page | #FFFFFF | #17171C | 페이지 배경 |
| bg-surface | #FFFFFF | #202027 | 카드, 시트, 모달 |
| bg-secondary | gray-50 | #26262E | 구분 영역 배경 |
| bg-fill | gray-100 | #2E2E38 | 입력창, 비활성 버튼 배경 |
| text-primary | gray-900 | #E7E9EE | 제목, 핵심 텍스트 |
| text-secondary | gray-600 | #9FA4AF | 설명, 캡션 |
| text-disabled | gray-400 | #5B5F6B | 비활성 |
| border | gray-200 | #33333E | 경계선, 구분선 |
| primary | blue-600 | blue-400 | 버튼, 링크, 강조 |
| primary-pressed | blue-700 | blue-500 | 눌림/hover |
| on-primary | #FFFFFF | #17171C | primary 채움 위의 글자·아이콘 |
| danger | red-600 | #FF6B6B | 오류, 삭제 |
| on-danger | #FFFFFF | #17171C | danger 채움 위의 글자·아이콘 |

### 1.5 CSS 변수 (코드 생성 시 이 블록을 그대로 사용)

```css
:root {
  --bg-page: #FFFFFF;
  --bg-surface: #FFFFFF;
  --bg-secondary: #F9FAFB;
  --bg-fill: #F2F4F6;
  --text-primary: #191F28;
  --text-secondary: #636D7A;
  --text-disabled: #B0B8C1;
  --border: #E5E8EB;
  --primary: #2368CC;
  --primary-pressed: #1B52A3;
  --primary-bg: #F0F6FE;
  --on-primary: #FFFFFF;
  --danger: #C92A37;
  --danger-bg: #FEF0F1;
  --on-danger: #FFFFFF;
  --success: #12793F;
  --warning: #A85B00;
}

[data-theme="dark"] {
  --bg-page: #17171C;
  --bg-surface: #202027;
  --bg-secondary: #26262E;
  --bg-fill: #2E2E38;
  --text-primary: #E7E9EE;
  --text-secondary: #9FA4AF;
  --text-disabled: #5B5F6B;
  --border: #33333E;
  --primary: #5D9CF3;
  --primary-pressed: #317FEE;
  --primary-bg: #1B2A45;
  --on-primary: #17171C;
  --danger: #FF6B6B;
  --danger-bg: #3A2226;
  --on-danger: #17171C;
  --success: #34C77B;
  --warning: #F5A93F;
}
```

다크 모드의 primary/danger 채움은 밝은 색이라 흰 글자를 얹으면 대비가 무너진다
(#5D9CF3 위 흰 글자 2.79:1). 채움 위 글자는 라이트/다크 모두 `--on-primary`,
`--on-danger`를 쓴다. 흰색을 직접 쓰지 않는다.

## 2. 타이포그래피

글꼴: **Pretendard** (fallback: `Pretendard, -apple-system, "Apple SD Gothic Neo", "Noto Sans KR", sans-serif`)

전 연령 대상이므로 본문 기준 크기를 일반보다 한 단계 크게 잡는다(17px).

| 토큰 | 크기/행간 | 굵기 | 용도 |
|------|----------|------|------|
| display | 28px / 38px | 700 | 온보딩, 결과 화면의 큰 메시지 |
| title-1 | 24px / 33px | 700 | 화면 제목 |
| title-2 | 20px / 28px | 600 | 섹션 제목, 다이얼로그 제목 |
| body-1 | 17px / 26px | 400 | 본문 기본 |
| body-1-bold | 17px / 26px | 600 | 본문 강조 |
| body-2 | 15px / 22px | 400 | 보조 본문, 목록 부가 정보 |
| caption | 13px / 19px | 400 | 캡션, 헬퍼 텍스트, 타임스탬프 |

규칙:
- 한 화면에 title급은 하나만 쓴다 (정보 위계 명확히)
- 굵기는 400/600/700 세 가지만 쓴다
- 색으로 위계를 만든다: 핵심 text-primary, 부가 text-secondary. 3번째 위계가 필요하면 화면을 나누는 것을 먼저 검토한다
- letter-spacing: 제목 -0.02em, 본문 0

## 3. 간격과 레이아웃

기본 단위 4px. 아래 스케일만 사용한다: **4, 8, 12, 16, 20, 24, 32, 40, 48**

밀도는 "여유롭게"가 기준:

| 항목 | 값 |
|------|-----|
| 화면 좌우 패딩 | 20px (모바일) / 24px (태블릿+) |
| 섹션 간 간격 | 32px |
| 카드 내부 패딩 | 20px |
| 목록 항목 높이 | 최소 56px |
| 제목과 본문 사이 | 8px |
| 본문과 버튼 사이 | 24px |
| 콘텐츠 최대 폭 (웹) | 640px (읽기 중심) / 1080px (대시보드) |

한 화면에는 한 가지 목적만 담는다. 섹션이 4개를 넘으면 화면 분리를 검토한다.

## 4. 형태 (Radius, Shadow)

모서리는 "살짝만 둥글게":

| 토큰 | 값 | 용도 |
|------|-----|------|
| radius-xs | 4px | 배지, 태그 |
| radius-sm | 6px | 입력창, 작은 버튼 |
| radius-md | 8px | 버튼, 카드 (기본값) |
| radius-lg | 12px | 모달, 바텀시트 |
| radius-full | 9999px | 아바타, 토글에만 |

그림자는 최소한으로. 경계는 그림자보다 배경색 차이와 border로 구분한다:

| 토큰 | 값 | 용도 |
|------|-----|------|
| shadow-none | — | 카드 기본 (배경/보더로 구분) |
| shadow-md | 0 4px 16px rgba(0,0,0,0.08) | 드롭다운, 팝오버 |
| shadow-lg | 0 8px 32px rgba(0,0,0,0.12) | 모달 |

## 5. 컴포넌트 규칙

### 버튼

| 종류 | 스타일 | 용도 |
|------|--------|------|
| Primary | primary 배경 + on-primary 글자 | 화면의 대표 행동. **한 화면에 하나만** |
| Secondary | primary-bg 배경 + primary 글자 | 보조 행동 |
| Ghost | 배경 없음 + text-secondary | 3순위 행동 (건너뛰기 등) |
| Danger | danger 배경 + on-danger 글자 | 삭제 등 파괴적 행동에만 |

- 크기: large 52px(화면 대표 행동, 하단 고정), medium 44px, small 36px
- 문구는 반드시 구체적 행동: "확인" ✗ → "변경 내용 저장" ✓
- 비활성 대신 가능하면 활성 상태로 두고 누르면 무엇이 부족한지 알려준다

### 입력창

- 높이 52px, bg-fill 배경, radius-sm, 포커스 시 primary 보더
- 레이블은 입력창 위에 항상 표시 (placeholder를 레이블 대용으로 쓰지 않는다)
- 헬퍼 텍스트(caption, text-secondary)로 형식 안내, 오류 시 danger 색으로 교체
- 민감 정보 입력 시 그 아래에 "왜 필요한지 + 어디에만 쓰이는지" 명시

### 오류 메시지

템플릿: `{원인} 때문에 {작업}하지 못했어요. {해결 행동}해 주세요.`

- 기술 코드(Error 500 등) 노출 금지, 사용자 탓 금지, 해결 방법 없는 통보 금지

### 다이얼로그 / 확인

- 파괴적 행동 전에만 확인을 요구한다. 되돌릴 수 있는 행동에는 확인 대신 실행 후 "실행 취소" 제공
- 확인 다이얼로그의 버튼도 구체적으로: "확인/취소" ✗ → "삭제하기/돌아가기" ✓
- 파괴적 행동 버튼은 항상 Danger 스타일 + 텍스트 레이블 (아이콘 단독 금지)

### 빈 화면 (Empty state)

- 무엇이 비었는지 + 채우면 무엇이 좋은지(가치) + 시작 행동 버튼 한 개

## 6. 모션

"자연스럽게 최소한":

| 항목 | 값 |
|------|-----|
| 기본 트랜지션 | 200ms, ease-out |
| 작은 상태 변화 (hover, press) | 150ms |
| 화면 전환, 모달 | 250ms |
| easing | cubic-bezier(0.3, 0, 0.2, 1) |

- 장식용 애니메이션(반복 재생, 튀는 효과)은 쓰지 않는다
- 사용자 입력에 대한 반응은 즉시 시작한다 (딜레이 금지)
- `prefers-reduced-motion` 설정 시 트랜지션 제거

## 7. 보이스톤 (UX Writing)

세부 규칙은 [guides/ux-writing.md](guides/ux-writing.md)를 따른다. 이 제품의 결정값:

| 항목 | 결정 |
|------|------|
| 말투 | 해요체 ("~해요", "~해 주세요") |
| 사용자 호칭 | 쓰지 않는다. 사용자 관점으로 쓴다 ("회원님의 계좌" ✗ → "내 계좌" ✓) |
| 이모지 | 쓰지 않는다 |
| 느낌표 | 축하 순간에만 최대 1개 |
| 민감 상황 (오류·거절·실패) | 차분하고 담백하게. 장난스러운 표현 금지 |

예시 톤:

```text
안내: 내 보험을 불러오려면 본인 인증이 필요해요.
성공: 저장했어요.
오류: 연결이 불안정해서 불러오지 못했어요. 잠시 후 다시 시도해 주세요.
빈 화면: 아직 등록된 카드가 없어요. 카드를 등록하면 지출을 한눈에 볼 수 있어요.
```

## 8. 접근성 최소 기준

- 텍스트 대비: 본문 4.5:1 이상, 큰 제목(24px 이상 또는 19px 이상 bold) 3:1 이상
- 본문 기준이 17px라 **버튼 레이블도 large text가 아니다 → 4.5:1을 적용한다**
- 채움 배경 위 글자는 `--on-primary` / `--on-danger`만 쓴다 (흰색 하드코딩 금지)
- -500 원색(#317FEE, #F04452, #ED8B00) 위에 글자를 얹지 않는다 — 모두 4.5:1 미달
- 터치 영역: 최소 44×44px
- 아이콘 단독 버튼에는 항상 접근성 레이블 제공. 파괴적 행동은 텍스트 병기
- 색만으로 상태를 구분하지 않는다 (오류는 색 + 문구)
- 폼 요소에는 항상 연결된 레이블

### 실측 대비값

색 토큰을 바꾸면 `python scripts/check-contrast.py`로 다시 검증한다.
이 스크립트는 §1.5의 CSS 블록을 직접 읽으므로 문서와 어긋나지 않는다.

| 조합 | 라이트 | 다크 |
|------|--------|------|
| on-primary / primary (버튼 레이블) | 5.35:1 | 6.39:1 |
| on-danger / danger (삭제 버튼) | 5.43:1 | 6.44:1 |
| primary / bg-page (링크) | 5.35:1 | 6.39:1 |
| primary / primary-bg (Secondary 버튼) | 4.92:1 | 5.13:1 |
| text-primary / bg-page | 16.56:1 | 14.70:1 |
| text-secondary / bg-page | 5.25:1 | 7.15:1 |
| text-secondary / bg-fill (입력창 헬퍼) | 4.76:1 | 5.37:1 |
| danger / danger-bg (오류 배너) | 4.90:1 | 5.27:1 |
| success / bg-page | 5.48:1 | 8.17:1 |
| warning / bg-page | 5.05:1 | 9.04:1 |

## 9. AI에게 UI를 시킬 때 함께 전달할 지시문

```text
위 DESIGN_SYSTEM.md를 따라 UI를 만들어줘. 특히:
1. 색은 시맨틱 토큰(CSS 변수)만 사용하고 라이트/다크 모두 지원할 것
   (채움 위 글자는 --on-primary / --on-danger. 흰색 하드코딩 금지)
2. Primary 버튼은 화면당 하나, 버튼 문구는 구체적 행동으로 쓸 것
3. 문구는 해요체, 이모지 없이, 사용자 호칭 없이 쓸 것
4. 오류 메시지는 원인 + 해결 방법 템플릿을 따를 것
5. 본문 17px 기준의 여유로운 밀도를 유지할 것
```
